doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Bienes Raices | #{view}
        link(rel="stylesheet", href="/css/app.css")
        block styles
    body.min-h-screen.bg-gray-50 
        if showNavbar && showNavbar == true
            include header 
        main(class="container mx-auto mt-10 px-2")
            //- layout inicial del componente padre
            block contenido
        //- cargar mapas 
        block scripts