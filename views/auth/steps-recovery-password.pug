extends  ../layout/index
block contenido
    div(class="p-10")
        h1(class="text-4xl my-5 font-extrabold text-center capitalize") Bienes
            span(class="font-normal") Raices
        h2(class="text-indigo-600 font-bold text-center capitalize text-xl")=view
        
        if errors
            each err in errors 
                div(class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative mt-1 text-xs" role="alert")
                    strong(class="font-bold p-2") Error!
                    code(class="block sm:inline")=err.msg 

        div(class="flex justify-center mx-auto max-w-md mt-10 w-1/2")
            div(class="bg-white p-4 shadow rounded-xl w-full")
                form(class="flex gap-3 flex-col" role="form" method="POST" action="/auth/confirm-recovery-password")
                    input(type="hidden" name="token" value=token)
                    div 
                        label(class="block text-gray-700 text-sm font-bold mb-2" for="new_password") new_password
                        //- el atrr name hace referencia al key del json que se va a enviar en el body de la peticion
                        input(class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline" id="new_password" type="password" name="new_password"  placeholder="new_password")
                    div 
                        label(class="block text-gray-700 text-sm font-bold mb-2" for="confirm_new_password") confirm_new_password
                        //- el atrr name hace referencia al key del json que se va a enviar en el body de la peticion
                        input(class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline" id="confirm_new_password" type="password" name="confirm_new_password"  placeholder="confirm_new_password")
                    div(class="my-3")
                        button(class="flex w-full justify-center rounded-md bg-indigo-600 px-3 py-1.5 text-sm font-semibold leading-6 text-white shadow-sm hover:bg-indigo-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600" type="submit") Enviar correo de recuperación
                        